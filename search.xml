<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Did Aristotle Use a Laptop? A Question Answering Benchmark with Implicit Reasoning Strategies阅读笔记</title>
    <url>/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="https://arxiv.org/pdf/2101.02235.pdf">论文链接</a></p>
<h1 id="摘要">摘要</h1>
<p>目前的多条推理数据集的一个重大缺陷是它们所要求的推理步骤都【显式地】表达在问题中。本文介绍StrategyQA，一个推理步骤【隐式地】包含在问题中，需要用策略来推导的QA基准测试。</p>
<p>StrategyQA一共包含2780个条目，每个条目包括：</p>
<ul>
<li>一个策略问题（推理步骤隐含）</li>
<li>若干子问题，是策略问题的分解（每个推理步骤一个子问题）</li>
<li>包含了每个子问题答案的维基百科文段</li>
</ul>
<p>回答是yes/no。</p>
<p>这些问题都很短而且覆盖了很大的范围，人类表现约为87%，而baseline则是66%。</p>
<a id="more"></a>
<h1 id="引入">引入</h1>
<img data-src="/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201131940802.png" class>
<p>现有的问题集一般把推理步骤显式展现在问题里，比如Q2"Was Aristotle alive when the laptop was invented?"，但在日常生活中，推理的步骤要更隐式，比如Q1"Did Aristotle use a laptop?"。事实上，后一个问题可以与前一个问题用完全相同的步骤来进行解答（如上图所示），但是它需要模型推理出“我需要对这两个时间进行比较”这样一个策略。</p>
<p>回答隐式的问题比起回答显式的问题要更困难。首先，问题和语境的重合程度比较低，获取语境本身就比较难；其次，问题比较短，使得模型利用问题文本取捷径更加困难。因此，作者提出了StrategyQA，要求模型利用策略来回答推理步骤隐含的问题，而且覆盖了相当多的人类在回答问题时使用的策略。</p>
<p>作者希望问题能尽可能地广泛，但是众包工人的<em>创造力</em>往往不尽人意，因此作者采取了一些方法来提升问题质量，比如给工人随机的维基百科条目、增加工人数量来减少单个人的工作量，以及在数据采集时就训练对抗模型并缓慢地提升难度。（考虑到我们的关注点是数据集本身，数据集采集方面的论述我不再记录）</p>
<h1 id="策略问题">策略问题</h1>
<h2 id="策略问题的要求">策略问题的要求</h2>
<p>本文中以维基百科作为训练语料。</p>
<p>策略问题需要具有以下特征：</p>
<ul>
<li>需要多步推理</li>
<li>可行性（从语料中可以找到答案，如“独眼莲适不适合用单片镜？”可以从维基百科中找到信息“单片镜是用来戴在一只眼睛上的”、“独眼莲只有一只眼”，而“Justin Bieber有没有一台Zune？”则不行）</li>
<li>隐式的</li>
<li>确定的（“汉堡是三明治的一种吗？”不行，“巧克力奶昔比香草味更好喝吗？”不行）</li>
</ul>
<img data-src="/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201144658579.png" class>
<h2 id="策略问题的分解">策略问题的分解</h2>
<p>每个策略问题可以分解为若干个步骤问题，每个问题对应一个基本的步骤。步骤问题中可能存在占位符，表示前面问题的结果。</p>
<img data-src="/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201145953846.png" class>
<p>没有截全，但是这两个例子足以解释。</p>
<p>斜体是输入中显式给出的信息，而粗体是输入中隐含的信息。</p>
<h1 id="数据采集过程">数据采集过程</h1>
<p>略过</p>
<h1 id="strategyqa-数据集">StrategyQA 数据集</h1>
<img data-src="/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201150132404.png" class>
<div data-align="center">
基本统计信息
</div>
<ul>
<li><p>Yes/No回答数目基本一致</p></li>
<li><p>问题比较短（相比HotpotQA和WebQuestions而言），而且重复度比较低</p></li>
<li><p>既要求多跳，又隐式包含推理信息</p></li>
<li><p>包含许多分类</p></li>
</ul>
<img data-src="/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201151737358.png" class>
<div data-align="center">
分类信息（最后一个好损）
</div>
<h1 id="实验和evaluation">实验和Evaluation</h1>
<h2 id="基线模型">基线模型</h2>
<p>回答StrategyQA的问题需要很多外部知识，仅仅用这两千个问题训练是远远不够的。</p>
<p>因此基线模型基于预训练的语言模型，在辅助数据集上finetune得到。具体地讲，是RoBERTa，在以下数据集上finetune：</p>
<ul>
<li>BoolQ</li>
<li>MNLI</li>
<li>Twenty Questions</li>
<li>DROP</li>
</ul>
<p>模型有两种配置：</p>
<ul>
<li><p>无语境：</p>
<p>只喂问题，输出0/1，用特殊的CLS token（？）</p></li>
<li><p>有语境：</p>
<p>用BM25从语料中抽取语境，把问题和抽取出的语境concat到一起，把512个token后面trunc调，然后输出0/1预测。</p></li>
</ul>
<h3 id="预测问题的分解">预测问题的分解</h3>
<p>在StrategyQA上finetune了一个BART（称为<span class="math inline">\(\text{BART}_{\text{DECOMP}}\)</span>），来输入问题，输出它的分解（也就是若干子问题）。</p>
<h3 id="基线模型-1">基线模型</h3>
<p>作者用<span class="math inline">\(\text{RoBERTa}\)</span>，一次在DROP、20Q和BoolQ上finetune。在DROP上训练时，首先用多输出头，然后再替换为单个布尔值输出。这个finetune过的模型被称作<span class="math inline">\(\text{RoBERTa}^{*}\)</span>。</p>
<p>作者用了以下模型作为基线：</p>
<ul>
<li><p><span class="math inline">\(\text{RoBERTa}^{*}\_{\phi}\)</span>，无语境finetune的<span class="math inline">\(\text{RoBERTa}^{*}\)</span></p></li>
<li><p><span class="math inline">\(\text{RoBERTa}^{*}_{\text{IR-Q}}\)</span>和<span class="math inline">\(\text{RoBERTa}_{\text{IR-Q}}\)</span>，分别是使用了对问题整体抽取语境的<span class="math inline">\(\text{RoBERTa}^{*}\)</span>和<span class="math inline">\(\text{RoBERTa}\)</span></p></li>
<li><p><span class="math inline">\(\text{RoBERTa}^{*}_{\text{IR-D}}\)</span>，对问题首先做了一次分解（上文的<span class="math inline">\(\text{BART}_{\text{DECOMP}}\)</span>），然后再抽取语境的<span class="math inline">\(\text{RoBERTa}^*\)</span></p></li>
</ul>
<p>后面几个oracle模型，又什么gold paragraph又什么gold decomposition的，没明白。图片放在这了。</p>
<img data-src="/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201153837758.png" class>
<p>结果如下：</p>
<img data-src="/2021/02/01/Did-Aristotle-Use-a-Laptop-A-Question-Answering-Benchmark-with-Implicit-Reasoning-Strategies%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201153909299.png" class>
<h2 id="结果分析">结果分析</h2>
<p><span class="math inline">\(\text{RoBERTa}^{*}_{\text{IR-Q}}\)</span>暴打<span class="math inline">\(\text{RoBERTa}_{\text{IR-Q}}\)</span>，说明在辅助数据集上finetune是很有用的，所以我们下文就只关心<span class="math inline">\(\text{RoBERTa}^{*}\)</span>了。</p>
<p>不用语境训练使得acc从53.9上升到了63.6，说明大型语言模型在相关数据集上嗯训也能训出东西来。</p>
<p>不过在<em>gold</em> paragraph（这到底是啥啊）上训练就能提升到70.7，说明高质量的预警抽取可以让模型跑得更有效。</p>
<p>而且加入decomposition之后进一步提升到72.0，说明分解有用（内心OS：但用处不大）</p>
<p>不在辅助数据集上finetune的话，即使用语境也不如在辅助数据集上finetune过但没有语境的模型。所以说没有辅助数据集，表现会很垃圾。</p>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>End-to-End Differentiable Proving阅读笔记</title>
    <url>/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="end-to-end-differentiable-proving">End-to-End Differentiable Proving</h1>
<p><a href="https://arxiv.org/pdf/1705.11040.pdf">论文链接</a></p>
<h2 id="写在前面的废话">写在前面的废话</h2>
<p>刚开始认认真真地看论文，只是看的速度实在是太慢了。感觉记录下来的东西有点类似于不正式的翻译，非常流水账，但是什么东西都不记又什么都记不住，左耳朵进右耳朵出了。 学长分配给的论文还有6篇，这篇也刚刚看完结构和LOSS，道阻且长……</p>
<h2 id="前置知识">前置知识</h2>
<h3 id="知识库自动补全automated-knowledge-base-completion"><a href="https://paperswithcode.com/task/knowledge-base-completion/latest">知识库自动补全</a>（automated Knowledge Base completion）</h3>
<p>给定知识库中已有的一部分信息，自动推导出缺失的信息。</p>
<h3 id="多跳的multi-hop">多跳的（multi-hop）</h3>
<p>与一步的逻辑推理相对，指<span class="math inline">\(A\, \wedge (A\to B)\,\wedge (B\to C) \, \wedge (C\to D)\Rightarrow D\)</span>这类需要经过多步的推理。</p>
<h3 id="次符号的subsymbolic">次符号的（subsymbolic）</h3>
<p>我还没理解。似乎是指粒度比符号更小？</p>
<h2 id="introduction">Introduction</h2>
<p>目前的知识库自动补全SOTA模型使用神经链预测模型(neural link prediction model)来学习符号的分布向量表示。向量表示允许模型通过相似性来对未知的事实进行泛化。</p>
<p>然而，神经链预测模型对于需要多个步骤进行预测的情况预测不佳（A是B的父亲，B是C的家长=&gt;A是C的祖父或祖母）。与之相反，符号定理证明器(symbolic theorem prover)正好可以处理这类问题。进一步，归纳逻辑程序设计(Inductive Logic Programming)利用这些证明器来从数据中学到可解释的规则并且在完成知识库的过程中进行应用。 但是，symbolic prover在subsymbolic表示的学习和相似度比较上能力比较差，这限制了他们对相似但不完全相同的询问进行泛化的能力。</p>
<p>尽管逻辑和机器学习的联系的问题已经由统计相关学习方法(statistical relational learning approaches)所解决了，以往的模型要么不支持次符号表示的推理，要么不是端到端训练出来的。神经多跳推理模型(neural multi-hop reasoning chain)通过将推理链encode到向量空间上去（也就是：在与答案进行比对之前，先迭代地将subsymbolic representation进行refine），在某种程度上解决了这个问题。但是它也有问题：可解释性差，而且难以显式地将领域内知识（例如作为某条规则）传递进去。</p>
<p>受到Prolog的反向链式算法和使用次符号表示的可微的归一化操作的方法，作者提出了Neural Theorem Prover。</p>
<a id="more"></a>
<h2 id="background">Background</h2>
<p>概念简介：</p>
<p>一个<code>atom</code>包含一个谓词和若干个项，因为我们考虑的是无函数的一阶逻辑，每个项只能是常量或者变量。例如，<span class="math inline">\([grandfatherOf, \textbf{Q}, BART]\)</span>是一个包含了谓词<code>grandfatherOf</code>和两个项（变量<code>Q</code>和常量<code>BART</code>）的<code>atom</code></p>
<p>一个<code>rule</code>形如<span class="math inline">\(H\text{:-}B\)</span>，其中H是一个<code>atom</code>，B是一系列<code>atom</code>的合取（可能为空），（代表B推出H）</p>
<p>一个不含变量的<code>rule</code>叫做<code>ground rule</code>，一个B为空的<code>rule</code>叫做<code>fact</code></p>
<p>一个<code>substitution set</code>形如<span class="math inline">\(\psi=\{X_1/t_1, X_2/t_2,...,X_N/t_N\}\)</span>，其中<span class="math inline">\(X_i\)</span>是变量，而<span class="math inline">\(t_i\)</span>是用于替换变量的值。将一个<code>substitution set</code>应用于一个<code>atom</code>即为将这个<code>atom</code>中的所有变量替换为<code>substitution set</code>中对应的替换值。</p>
<p>给定一个query（也称作goal），以<span class="math inline">\([grandfatherOf, \textbf{Q}, BART]\)</span>为例，我们可以用Prolog's backward chaining algorithm来找到Q适合的替换值。</p>
<p>总地来说，这个算法基于两个函数，OR和AND。</p>
<p>首先，OR遍历所有的<code>rule</code>（包括<code>fact</code>），将每个<code>rule</code>的H和goal进行合并（谓词相同才可以合并，用goal里的对象对应地替换<code>rule</code>里的对象），同时更新<code>substitution set</code>。因为只要有一条<code>rule</code>能够成功合并就足够了，所以这个操作称作OR。</p>
<p>当合并成功后，OR会调用AND来证明<code>rule</code>的body中的所有<code>rule</code>（也叫作subgoal）。AND首先将<code>substitution set</code>应用于第一个<code>atom</code>，然后继续调用OR来证明他；随后对每一个body中的<code>atom</code>都做相同的事情。只有所有的<code>atom</code>都被证明才可以说明goal被证明，所以这个操作叫做AND。</p>
<p>举一个例子：</p>
<p>goal是<span class="math inline">\([grandfatherOf, \textbf{Q}, BART]\)</span>。</p>
<p>有一条<code>rule</code>是<span class="math inline">\([grandfatherOf, \textbf{X}, \textbf{Y}]\text{:-}[[fatherOf, \textbf{X}, \textbf{Z}],[parentOf, \textbf{Z}, \textbf{Y}]]\)</span></p>
<p>这样OR操作合并后得到的subgoal就是<span class="math inline">\([fatherOf,\textbf{Q},\textbf{Z}]\)</span>和<span class="math inline">\([parentOf,\textbf{Z}, BART]\)</span>，然后再用AND操作分别去证明。</p>
<p>个人感觉英文wiki上的算法说明更明确易懂……</p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210128224251578.png" class>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210128224319624.png" class>
<h3 id="differentiable-prover">Differentiable Prover</h3>
<p>我们将NTP的构建表述为一系列<code>module</code>的构建。每个<code>module</code>输入一些离散的对象（如<code>atom</code>和<code>rule</code>）和一个<code>proof state</code>，同时输出若干新的<code>proof state</code>。</p>
<p>一个<code>proof state</code><span class="math inline">\(S = (\psi, \rho)\)</span>由<code>substitution set</code> <span class="math inline">\(\psi\)</span>和神经网络<span class="math inline">\(\rho\)</span>构成。其中<span class="math inline">\(\psi\)</span>是截至目前的<code>substitution set</code>，<span class="math inline">\(\rho\)</span>会输出一个（部分）证明的<code>success score</code>（是实数）。尽管离散的对象和<code>substitution set</code>都只在建立网络的过程中用到，一旦网络建立完成，在训练和测试的时候对于不同的proof我们就可以计算连续的proof <code>success score</code>。</p>
<p><code>module</code>由离散的对象和<code>substitution set</code>实例化。他们构成了一个表示（部分）证明的<code>success score</code>的神经网络，同时递归地实例化子<code>module</code>来继续证明。</p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210128234749898.png" class>
<p>所有<code>module</code>都共享以下声明：<span class="math inline">\(D\times S\to S^N\)</span>，其中D是控制网络构建的域，S是<code>proof state</code>的域，N是输出<code>proof state</code>的数目。进一步地，我们记<span class="math inline">\(S_\psi\)</span>为<code>proof state</code> S对应的<code>substitution set</code>，<span class="math inline">\(S_p\)</span>为它对应的神经网络</p>
<h4 id="合并模块">合并模块</h4>
<p>在backward chaining中，两个<code>atom</code>的合并是核心（例如，一个是我们要证明的goal，另一个是一条<code>rule</code>的<code>rule head</code>）。在朴素的backward chaining中，两个非变量的符号要检查是否一致——一致才可以合并，否则直接退出。然而，我们希望的是让它对于不完全一致而含义类似（e.g. <code>grandfatherOf</code>和<code>grandpaOf</code>）的也进行合并，因此我们通过神经网络来衡量两个符号在向量空间中的相似性。</p>
<p>unify模块就用来更新<code>substitution set</code>并创建一个可以比较非变量符号的向量表示的相似性的神经网络。它的签名形如<span class="math inline">\(L\times L\times S\to S\)</span>，其中L是由所有项的列表构成的集合（L中的元素都是包含若干项的列表），S是<code>proof state</code>的定义域。unify模块接受两个<code>atom</code>（表示为项的集合形式）和一个上游<code>proof state</code>，返回一个下游<code>proof state</code>。</p>
<p>unify模块首先遍历两个<code>atom</code>并且比较这些符号，如果一个符号是变量那么更新<code>substitution set</code>；否则将他们通过一个径向基函数(<a href="https://en.wikipedia.org/wiki/Radial_basis_function">radial basis function</a>)核，作者选取的超参数<span class="math inline">\(\mu=\frac{1}{\sqrt{2}}\)</span></p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130092554431.png" class>
<p>具体的unify规则如上所示，如果两个<code>atom</code>都空，那么输入<code>proof state</code>不变；如果只有一个为空，那么unify失败；如果两个都不为空，那么按照下面的两个公式逐元素进行unify。</p>
<p>其中<span class="math inline">\(S&#39;\)</span>是新<code>proof state</code>，<span class="math inline">\(\nu\)</span>是变量符号的集合，<span class="math inline">\(h/g\)</span>表示用值g替换变量h（也就是更新<code>substitution set</code>的过程）。这里的<span class="math inline">\(S_\rho&#39;\)</span>实际上是一个神经网络，min里面的值是【如果将这组输入喂给神经网络那么神经网络会输出的结果】。</p>
<p>注意到<span class="math inline">\(S_\rho\)</span>的值一定落在<span class="math inline">\((0,1]\)</span>上。</p>
<h4 id="or模块">OR模块</h4>
<p>or模块的签名为<span class="math inline">\(L\times \mathbb{N}\times S\to S^N\)</span>，其中L是所有goal <code>atom</code>的集合，<span class="math inline">\(\mathbb{N}\)</span>是正整数集（指明神经网络最长的推理长度），S是输入<code>proof state</code>，输出若干个<code>proof state</code>。</p>
<p>基于unify模块，or模块的定义是</p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130100730881.png" class>
<p>类似backward chaining中的做法，对每个<code>rule</code>取出head去unify，然后对unify得到的东西去做and操作。</p>
<h4 id="and模块">and模块</h4>
<p>首先定义一个辅助函数substitute，只要可能就会在<code>atom</code>中将变量替换（能换的尽量换）</p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130101058127.png" class>
<p>and模块的签名签名为<span class="math inline">\(L\times \mathbb{N}\times S\to S^N\)</span>，其中L是所有<code>goal atom</code>的集合，<span class="math inline">\(\mathbb{N}\)</span>是正整数集（指明神经网络最长的推理长度），S是输入<code>proof state</code>，输出若干个<code>proof state</code>。（和or一样）</p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130101519851.png" class>
<p>如果S是FAIL，或者剩余的步数是0，那么and操作都会失败。如果<code>atom</code>集合为空，那么直接将输入<code>proof state</code>输出。否则将<code>atom</code>集合中的每个元素拿出来，经过substitute替换后用or去递归建立神经网络。hexo</p>
<h4 id="聚合">聚合</h4>
<p>最终通过以下公式计算<code>success score</code></p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130102203392.png" class>
<h3 id="训练">训练</h3>
<p>NTP loss用负对数定义</p>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130102950882.png" class>
<img data-src="/2021/01/30/End-to-End-Differentiable-Proving%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130103012084.png" class>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>HOTPOTQA: A Dataset for Diverse, Explainable Question Answering阅读笔记</title>
    <url>/2021/02/02/HOTPOTQA:%20A%20Dataset%20for%20Diverse,%20Explainable%20Multi-hop%20Question%20Answering%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="https://www.aclweb.org/anthology/D18-1259.pdf">论文地址</a></p>
<h1 id="引入">引入</h1>
<p>现有的QA数据集大多数并不涉及复杂的推理过程（只在一个文段内推理，或者只需要一步推理）。</p>
<p>那些需要多跳推理的数据集，则主要围绕已有的知识库构建，限制了其多样性（问题一定围绕着知识库的架构来走），问题的解答也很单一。</p>
<p>另外，问题的监督粒度也太粗，只能针对问题来监督回答是否正确。</p>
<p>基于这三个问题，作者提出了HotpotQA数据集，特点在于：</p>
<ul>
<li>多跳推理</li>
<li>不基于任何的范式，更加自然</li>
<li>引入了一类全新的“实体比较”类问题，可以测试模型在常识方面的理解</li>
</ul>
<a id="more"></a>
<h1 id="数据采集">数据采集</h1>
<p>问题的提出基于文本而非知识库，保证其多样性。</p>
<h2 id="实体比较类问题">实体比较类问题</h2>
<p>两个实体在某一属性方面进行比较。</p>
<p>例如“谁在NBA效力过的球队更多，乔丹还是科比？”需要对实体<code>乔丹</code>和实体<code>科比</code>在<code>效力过的NBA球队数目</code>上进行比较，然后选出一个进行回答。</p>
<p>另一种实体比较类问题是yes/no问题，比如“铁娘子和AC/DC乐队来自同一个国家吗？”</p>
<h2 id="采集支持信息">采集支持信息</h2>
<p>比起对每个问题只提供一个答案，作者还对每个问题都列出了一个<code>supporting fact</code>集合，从集合中可以获取回答问题所需的所有知识。</p>
<p>这一方面可以提供更强力的监督，另一方面可以通过让模型预测<code>supporting fact</code>然后和真实的相比较来衡量模型的可解释性。</p>
<h1 id="数据集">数据集</h1>
<img data-src="/2021/02/02/HOTPOTQA:%20A%20Dataset%20for%20Diverse,%20Explainable%20Multi-hop%20Question%20Answering%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210201235641909.png" class>
<div data-align="center">
对数据集内问题的划分
</div>
<p>大多数答案是文段内的实体，同时也有其他属性如日期、数字和形容词之类。</p>
<p>约42%的问题需要链式推理（需要找到桥接实体来实现第二步推理），有一些问题依赖桥接实体的属性，还有一些问题需要通过同时满足多个属性来找到答案实体。这些都是多条推理的形式。</p>
<img data-src="/2021/02/02/HOTPOTQA:%20A%20Dataset%20for%20Diverse,%20Explainable%20Multi-hop%20Question%20Answering%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210202000140730.png" class>
<h1 id="实验">实验</h1>
<p>作者采取了<a href="https://arxiv.org/pdf/1710.10723.pdf">这篇文章</a>内的模型作为基线模型，但加上了很多新鲜技巧，例如字级模型、自注意力、双向注意力等。</p>
<img data-src="/2021/02/02/HOTPOTQA:%20A%20Dataset%20for%20Diverse,%20Explainable%20Multi-hop%20Question%20Answering%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210202002327969.png" class>
<p>为了回答问题，作者引入了一个三向分类器用来输出：yes/no/用文本回答问题。在回答时，首先确定问题的回答是不是yes/no。是就直接输出，不是就进一步用最高概率的词拿出来作为文本回答。</p>
<p>模型用两种基准测试来衡量。</p>
<p>在Distractor测试中，为了测量模型应对噪声的能力，喂给模型的10个文段中有两个是“金”文段（即用来构筑问题的文段本身），而剩下的8个是噪声，10个文段打乱后投喂给模型。</p>
<p>在Full wiki测试中，为了测量模型在实际应用中的效果，测试时将向模型投喂所有的wikipedia页面的开头几段，并不指定或加入金文段。</p>
<img data-src="/2021/02/02/HOTPOTQA:%20A%20Dataset%20for%20Diverse,%20Explainable%20Multi-hop%20Question%20Answering%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210202003325551.png" class>
<p>人类在测试中还是吊打了基线的……</p>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2021/01/22/Hello-World/</url>
    <content><![CDATA[<p>忙里偷闲把博客建起来了，hexo确实太方便了。 希望这个blog可以督促自己学习和写整理，能帮到屏幕前的你的话那再好不过了。 总而言之—— Hello, world!</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Hello, world!&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>杂谈</category>
      </categories>
      <tags>
        <tag>杂谈</tag>
      </tags>
  </entry>
  <entry>
    <title>LEARNING A SAT SOLVER FROM SINGLE-BIT SUPERVISION 阅读笔记</title>
    <url>/2021/01/30/LEARNING-A-SAT-SOLVER-FROM-SINGLE-BIT-SUPERVISION-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="https://arxiv.org/pdf/1802.03685.pdf">论文地址</a></p>
<h1 id="引入">引入</h1>
<p>作者希望用神经网络来解决SAT问题，提出了NeuroSAT模型。</p>
<p>NeuroSAT在训练过程中对每个问题的监督只有“可满足”和“不可满足”两种状态。测试时，每次给一个SAT问题P，NeuroSAT就会给出这个问题是否可满足，而且如果一个问题被判定为可满足，我们<strong>几乎总是</strong>可以从激活函数中找到一组合法的值代入。</p>
<p>他们又对模型进行了修改，输入的是有少量矛盾的不可满足问题，让模型自己去找到矛盾并改正，他们把这个修改叫NeuroUNSAT。</p>
<a id="more"></a>
<h1 id="问题建立">问题建立</h1>
<p>SAT问题的定义：存在一组值代入后使得公式P为真，那么P是可满足的；否则P是不可满足的。</p>
<h1 id="模型">模型</h1>
<p>SAT问题都具有比较简单的句法，因此用RNN之类的东西可以直接encode进向量空间里。但是，命题逻辑的语义里包含了丰富的不变性，如排列不变性（<span class="math inline">\(P\wedge Q = Q \wedge P\)</span>）和否定不变性（<span class="math inline">\(P\)</span>和<span class="math inline">\(\neg P\)</span>可满足性相同），这些不变量用句法级别的encoding会被忽略。</p>
<p>由此作者提出了他们的架构NeuroSAT，既保留了排列不变性，也保留了否定不变性。对于一个SAT问题，每个字面量和每个子句(clause)都被encode为无向图中的一个节点，如果某个字面量出现在了一个子句中，那么把他们对应的节点连起来。NeuroSAT通过不断地把“信息”在图中传来传去对向量空间表示进行不断的精化。在任何一个时间点，每个字面量和每个子句都有一个embedding。每次迭代由两部分组成：首先，每个子句都接收到它的邻居（字面量）节点的信息，并且依据这个信息更新自己的embedding。然后，每个字面量都接收到它的邻居（子句）和它的补节点（对于字面量<span class="math inline">\(P\)</span>，它会收到来自<span class="math inline">\(\overline{P}\)</span>的信息，反之亦然）的信息，并且依据这个信息更新自己的embedding。</p>
<img data-src="/2021/01/30/LEARNING-A-SAT-SOLVER-FROM-SINGLE-BIT-SUPERVISION-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210130133955785.png" class>
<p>左半部分是迭代的第一步，右半部分是第二步。</p>
<p>具体地来讲，模型用两个向量<span class="math inline">\(L_{init}, C_{init}\)</span>（都是<span class="math inline">\(\mathbb{R}^d\)</span>）初始化，包含三个多层感知机<span class="math inline">\(L_{msg}, C_{msg}, L_{vote}\)</span>和两个有Layer-Norm的LSTM <span class="math inline">\(L_u, C_u\)</span>。</p>
<p>在每个时间点<span class="math inline">\(t\)</span>，我们都用一个矩阵<span class="math inline">\(L^{(t)}\in \mathbb{R}^{2n\times d}\)</span>来表示字面量的embedding（一共n个字面量，加上补就有2n个，每个的embedding维度都是d），一个矩阵<span class="math inline">\(C^{(t)}\in \mathbb{R}^{m\times d}\)</span>来表示子句的embedding（一共m个子句，每个的embedding维度都是d）。他们分别用<span class="math inline">\(L_{init}\)</span>和<span class="math inline">\(C_{init}\)</span>堆起来进行初始化。同时，还有LSTM使用的隐状态<span class="math inline">\(L^{(t)}_h\)</span>和<span class="math inline">\(C_h^{(t)}\)</span>（尺寸和上面的对应），初始化为零矩阵。</p>
<p>定义矩阵<span class="math inline">\(M\)</span>为图的关联矩阵（原文叫邻接矩阵，但我记得离散2里把点和边的连通性矩阵叫关联矩阵，anyway），即<span class="math inline">\(M_{ij}=1\)</span>当且仅当第<span class="math inline">\(i\)</span>个字面量<span class="math inline">\(l_i\)</span>出现在子句<span class="math inline">\(c_j\)</span>中。</p>
<p>定义<code>Flip</code>操作，每次接受一个矩阵L，将L中每个字面量对应的行替换为它的否定对应的行（即如果矩阵L的四行分别对应<span class="math inline">\(P, Q, \neg P, \neg Q\)</span>，那么<code>Flip</code>后的矩阵四行分别对应<span class="math inline">\(\neg P, \neg Q, P, Q\)</span>）然后输出。</p>
<p>一次迭代过程表示如下（注意和上面文字描述的版本相对应）：</p>
<img data-src="/2021/01/30/LEARNING-A-SAT-SOLVER-FROM-SINGLE-BIT-SUPERVISION-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210131101904684.png" class>
<p>第一行是更新子句矩阵，用关联矩阵去乘<span class="math inline">\(L_{msg}\)</span>作用下的<span class="math inline">\(L^{(t)}\)</span>，每个子句就只受到它相关的字面量的影响。</p>
<p>第二行是更新字面量矩阵，除了隐状态<span class="math inline">\(L_h^{(t)}\)</span>和<span class="math inline">\(M\text{C}_{msg}(C^{t+1})\)</span>以外，他还引入了<code>Flip(L(t))</code>来引入否定不变性。</p>
<p>在<span class="math inline">\(T\)</span>次迭代后（一开始设置的最长迭代步数），用最后的一个全连接<span class="math inline">\(L_{vote}(L^{(T)})\)</span>来得到结果向量<span class="math inline">\(L_*^{(T)}\in\mathbb{R^{2n}}\)</span>，每个字面量得到一个标量值；再将这个向量取平均得到<span class="math inline">\(y^{(t)}=\text{mean}(L_*^{(T)})\in \mathbb{R}\)</span>，通过训练使得<span class="math inline">\(y^{(T)}\)</span>和真实标签<span class="math inline">\(\phi(P)\)</span>之间的sigmoid cross-entropy loss最小。</p>
<p>这个模型的好处：</p>
<ul>
<li>在架构设计上引入了排列不变形和否定不变性</li>
<li>与问题规模无关，可以处理各种规模的问题</li>
</ul>
<h1 id="训练数据">训练数据</h1>
<p>作者希望让模型有比较好的泛化能力，而不只是从某些领域内学得很好而其他地方很烂。</p>
<p>因此作者创建了一个问题集<span class="math inline">\(\mathbf{SR}(n)\)</span>，由包含<span class="math inline">\(n\)</span>个变量的SAT问题对组成，每对中一个是可满足的，一个是不可满足的，而且他们只在一个子句中对一个字面量取否定就可以相互转换（换言之，加上/去掉一个<span class="math inline">\(\neg\)</span>，这两个问题就一模一样）。</p>
<p>怎么取的我就不关心了。</p>
<h1 id="预测可满足性">预测可满足性</h1>
<p>首先在<span class="math inline">\(\mathbf{SR}(40)\)</span>上做（虽然这种东西对于现代SAT求解器来说很容易，但在ML方面来看仍然是困难的）。</p>
<p>朴素LSTM直接歇逼，准确率不足50%（即不如瞎蒙）；MiniSAT求解器平均需要backjump 10次（这意味着拆十次存在量词/全称量词），做超过100次基本逻辑推理。</p>
<p>NeuroSAT用<code>d=128</code>，每个多层感知机都有3个隐层（也即一共5层？），Layer-Norm的参数选<span class="math inline">\(10^{-10}\)</span>，跑26次迭代，用ADAM优化器，学习率<span class="math inline">\(2\times 10^{-5}\)</span>，clip rate取0.65来全局地剪切梯度（这是不是那个dropout？）。为了方便训练，他们训的是<span class="math inline">\(\mathbf{SR}(\mathbf{U}(10, 40))\)</span>，但在<span class="math inline">\(\mathbf{SR}(40)\)</span>上测试，得到了85%的准确度。接下来作者试图了解如何从被认为是可满足的问题中decode出一组解。</p>
<h1 id="解码满足条件">解码满足条件</h1>
<img data-src="/2021/01/30/LEARNING-A-SAT-SOLVER-FROM-SINGLE-BIT-SUPERVISION-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210131111537138.png" class>
<p>上图中，白色代表中立，蓝色表示倾向于不可满足，红色表示倾向于可满足，颜色深浅表示置信度高低。</p>
<p>因此我们可以看出，一开始网络整体以较低的置信度倾向于不可满足，但从一个转折点开始，有的字面量开始以较高的置信度倾向于可满足，而且很快所有字面量都开始以高置信倾向于可满足，随后网络收敛。</p>
<p>在正确地被判定为可满足的问题里，NeuroSAT的表现都差不太多。在NeuroSAT判定为不可满足的问题里，自始至终都是低置信的不可满足——它从来不会判定一个问题是高置信地不可满足。这意味着NeuroSAT会搜寻一个可满足性的【凭证】，只要找到就会开始判定可满足。</p>
<p>作者认为虽然引入了n个字面量的否定，但是从结果来看NeuroSAT对每个字面量还是只用了一个bit来encode。</p>
<p>如果把高维的字面量Embedding用PCA（主成分分析）降维投影到二维，那么可以发现，上文对应的转折点前红点（支持可满足）和蓝点（支持不可满足）是无法用一条线进行划分的，而转折点后则是可以轻易划分的。</p>
<img data-src="/2021/01/30/LEARNING-A-SAT-SOLVER-FROM-SINGLE-BIT-SUPERVISION-%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210131113320182.png" class>
<p>因此作者认为，在转折点后挑一个时间点做PCA，然后红蓝点就分别对应了字面量取值的1和0，由此就找到了一组可行解。</p>
<h1 id="向其他问题集泛化">向其他问题集泛化</h1>
<h2 id="规模更大的问题">规模更大的问题</h2>
<p>在小规模问题上训练，在大规模问题上推理，如果增加迭代次数，那么效果会提升（内心OS：MiniSAT也要backjump更多次啊 你这不是废话吗）。</p>
<p>在<span class="math inline">\(\mathbf{SR}(\mathbf{U}(10,40))\)</span>上训练的网络，在<span class="math inline">\(\mathbf{SR}(200)\)</span>上也可以做，只是最好最好就是25%了……</p>
<h2 id="不同类型的问题">不同类型的问题</h2>
<p>用不同的生成随机图的方法整了一堆10个点、17条边的图，出了好多不同类型的问题比如图染色、domaining-set problem（我没查到这是啥??）、完全图搜索、点覆盖等等，最后保留了4888个问题。</p>
<p>跑了512次迭代的NeuroSAT达到了85%的准确度。</p>
<h1 id="寻找unsat-core">寻找UNSAT core</h1>
<p>在<span class="math inline">\(\mathbf{SR}(\mathbf{U}(10,40))\)</span>上训练的NeuroSAT找可行解比较好，但不会给出不可行的证明，它没有暴力遍历可能性，而是随机地去撞大运。</p>
<p>但是作者又构造了一种新的数据集叫<span class="math inline">\(\mathbf{SRC}(n,u)\)</span>，和<span class="math inline">\(\mathbf{SR}(n)\)</span>类似，但是每个不可实现的问题都有一个已经不可满足的子句<span class="math inline">\(u\)</span>（叫unsat core）（这里unsat core是只要把一个<span class="math inline">\(\neg\)</span>加上或去掉就会变回可满足的子句）</p>
<p>在这种集合上训练的NeuroSAT（叫做NeuroUNSAT）会一上来就高置信地支持不可满足，然后在一个转折点开始低置信地支持可满足（如果判定可满足）或者在某个/某些字面量上以极其高的置信支持不可满足（如果判定不可满足）。</p>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>NLProlog: Reasoning with Weak Unification for Question Answering in Natural Language阅读笔记</title>
    <url>/2021/01/31/NLProlog-Reasoning-with-Weak-Unification-for-Question-Answering-in-Natural-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="https://arxiv.org/pdf/1906.06187.pdf">论文地址</a></p>
<h1 id="摘要">摘要</h1>
<p>作者将神经方法和逻辑推理结合起来，给出了一个新模型来处理自然语言上的多跳推理。作者提出，还是应用Prolog prover进行推理，但是应用了一个基于预训练语句encoder的相似度函数，通过反向传播来fine-tune相似度函数的表示。这样就可以在自然语言上实现基于规则的推理，也可以引入领域内的特定知识。</p>
<a id="more"></a>
<h1 id="引入">引入</h1>
<p>首先介绍了一下什么是多跳推理。已知苏格拉底生于雅典、雅典在希腊，问苏格拉底的出生地是哪里？，回答雅典则是朴素的答案匹配，回答希腊则意味着需要经过一步逻辑推理，这就是多跳推理。</p>
<p>以往的自然语言多跳推理一般采用神经模型，可以处理自然语言的多样性和模糊性，但可解释性很差，而且需要大量数据才能拥有泛化能力，而且一直没能解决如何引入领域内已有知识这个问题。</p>
<p>与之相反，基于规则的逻辑推理模型就非常易于解释，而且在小数据下泛化能力也非常强，但是对噪声的鲁棒性非常差，所以对自然语言这种模糊的东西很难处理。</p>
<p>因此作者提出了NLProlog，将符号推理器、规则学习方法和实体表示结合起来，从而实现一个在自然语言上进行基于规则的多跳推理的模型。它是部分可解释的，而且很容易引入先验知识。同时，它不需要自然语言被转化为逻辑形式作为中介。</p>
<p>NLProlog的核心是类似Prolog的backward chaining定理证明器，只不过对symbol的比较（unification过程）被替换为可微的（也就是神经的）相似度函数，对两个symbol的分布表示的相似程度进行衡量。这个过程由基于预训练的sentence embedding的、端到端的sentence encoder完成，然后在下游任务上finetune即可，这个下游任务的finetune过程也就是引入领域内的逻辑规则的过程。</p>
<p>作者在两个QA数据集上evaluate了这个模型，效果不戳。</p>
<h1 id="相关工作">相关工作</h1>
<p>我先跳过这里，感觉和正文关系有限</p>
<h1 id="背景">背景</h1>
<p>每个Prolog程序都包含若干以下形式的霍恩子句： <span class="math display">\[
h(f_1^h, ...,f_n^h) \Leftarrow p_1(f_1^1,...,f_m^1)\,\wedge\,...\,\wedge\,p_B(f_1^B,...,f_l^B)
\]</span> 其中<span class="math inline">\(h, p_i(1\leq i\leq B)\)</span>是谓词符号，<span class="math inline">\(f_i^j\)</span>要么是函数，要么是变量。所有函数的集合称为<span class="math inline">\(\mathcal{F}\)</span>，所有谓词的集合称为<span class="math inline">\(\mathcal{P}\)</span>。<span class="math inline">\(h(f_1^h, ...,f_n^h)\)</span>被称为<code>head</code></p>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>PROVER: Proof Generation for Interpretable Reasoning over Rules阅读笔记</title>
    <url>/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="https://arxiv.org/pdf/2010.02830.pdf">论文链接</a></p>
<p>我家鲨鱼觉得我太摸了，亲自跑到电脑前面来替我写论文笔记(×</p>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/shark.jpg" class>
<p>废话：</p>
<p>看了这三篇我才反应过来我看的顺序好像有点问题……</p>
<p>Soft Reasoner是最先的，这篇PRover是第二个，然后才是ProofWriter（那个基于T5-11B，全程diss PRover的）。</p>
<p>反正是用神经方法做推理……玄而又玄，众妙之门</p>
<h1 id="摘要">摘要</h1>
<p>作者提出了PRover，一个可解释的、基于Transformer的模型，既可以在自然语言规则库上进行推理（判断对错），也可以写出推理结论的证明。</p>
<a id="more"></a>
<h1 id="引入">引入</h1>
<p><a href="https://zcy.moe/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/">这篇文章</a>提出了在自然语言的规则库上进行推理的全新任务，还给了一个模型。但作者认为，仅仅给出判断是不够的，还应该写出证明。</p>
<p>因此他们提出了PRover模型，既可以在自然语言规则库上判断给定事实的真假，还可以写出（模拟的）证明过程。</p>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204121859197.png" class>
<p><a href="https://zcy.moe/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/#bulb">沙雕灯泡</a>没了（悲）</p>
<p>在本文中，证明以连接了相关的规则和事实的有向图表示。</p>
<p>PRover的架构由三个模块构成，一个QA模块用来输出0/1预测（用来回答）、一个节点模块用来选择证明中用到的规则和事实、一个边模块用来预测节点模块选出的规则和事实的关系（用来写证明）。</p>
<h1 id="方法">方法</h1>
<p>PRover接受的输入是一个语境<span class="math inline">\(C\)</span>（包括事实F和规则R）和一个与语境相关的问题<span class="math inline">\(Q\)</span>，输出一个答案<span class="math inline">\(A \in \{True,False\}\)</span>和一个证明P。</p>
<h2 id="证明的表示">证明的表示</h2>
<p>证明P被表示为一个有向图<span class="math inline">\(\mathcal P=(\mathcal{N},\mathcal{E})\)</span>，<span class="math inline">\(\mathcal{N}\)</span>表示节点，<span class="math inline">\(\mathcal{E}\)</span>表示边。每个节点要么是事实<span class="math inline">\(f\in F\)</span>，要么是规则<span class="math inline">\(r \in R\)</span>，要么是NAF（Negation As Failure，下文将解释）节点。</p>
<p>每条边要么由事实（或NAF节点）指向规则，要么由规则指向规则。这条有向边意味着这个事实、NAF或规则被那个被指向的规则使用了。</p>
<p>数学表示如下： <span class="math display">\[
\mathcal{P} = (\mathcal N, \mathcal E) \\
\mathcal N \subset R\,\cup\,F\,\cup\,NAF \\
\mathcal E \subset \mathcal N \times \mathcal N
\]</span> <img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204122948219.png" class></p>
<p>以上是三类证明的例子：</p>
<ul>
<li><p>没有用到NAF的成功证明（Q1、Q2）</p></li>
<li><p>用到了NAF的成功证明（Q4、Q5）</p>
<p>NAF是一条非单调的推理规则，它的作用是从“不能证明S”得到“S的否定”</p>
<p>（这跟之前看到的CWA是一致的，也就是整不出来就是假）</p></li>
<li><p>失败的证明（Q3）</p>
<p>如果一个陈述不能由给定的规则库推理出来，则生成一个证明树上最浅的分支。</p></li>
</ul>
<h2 id="任务描述">任务描述</h2>
<p>每个训练用例都形如<span class="math inline">\((C_i=\{F_i, R_i\}, Q_i, A_i, P_i)\)</span>，即一个语境、一个问题、一个正确答案、一个证明。</p>
<p>要生成证明图，需要</p>
<ol type="1">
<li>找到证明中要用到的节点</li>
<li>找到节点之间的边的联系</li>
<li>验证一些全局约束（比如连通性）来确保证明是合理的</li>
</ol>
<p>对于第一个要求，我们对于每个节点都预测一个0/1标签来表明它在证明中出现/不出现。对于第二个要求，我们对于边预测0/1标签来表明它出现/不出现。对于第三个要求，我们在训练和推理的时候都施加约束。训练时，我们mask掉以下三类边的标签：自环、没有出现（在证明中）的节点之间的边，以及事实到事实和规则到事实的边。</p>
<p>这样就施加了一个语义约束，保证边在选定的节点集内部是连贯的，同时也简化了学习的过程。</p>
<h2 id="prover">PRover</h2>
<p>PRover基于RoBERTa，由QA模块、节点模块和边模块三部分构成，其中QA模块和RuleTakers完全一致。</p>
<p>向RoBERTa投喂的输入由语境和问题构成，具体形式如下：</p>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204132109349.png" class>
<h3 id="qa模块">QA模块</h3>
<p>RoBERTa的输出是语境中每个token的embedding，同时还有一个[CLS]的embedding（记为<span class="math inline">\(t_{[CLS]}\)</span>）。</p>
<p>我们用以下公式计算答案的概率： <span class="math display">\[
P_{QA}=\sigma(H_{QA}(t_{[CLS]}))
\]</span> 其中<span class="math inline">\(\sigma\)</span>是softmax函数，而<span class="math inline">\(H_{QA}\)</span>是一个带有dropout概率p的双层全连接。</p>
<h3 id="节点模块">节点模块</h3>
<p>对于语境中的每个成员（要么是规则要么是事实，统称为<span class="math inline">\(RF_i\)</span>），我们将它包含的所有token embedding取平均值得到这个规则或事实的embedding，即 <span class="math display">\[
t_{RF_i}=MP(\{t_{w_j^{(i)}}\}_{j=1}^m)
\]</span> （<span class="math inline">\(\{w_j^{(i)}\}_{j=1}^m\)</span>是<span class="math inline">\(RF_i\)</span>对应的所有token，<span class="math inline">\(\{t_{w_j^{(i)}}\}_{j=1}^m\)</span>是这些token对应的embedding，MP是mean pooling）</p>
<p>对于NAF节点，我们有特殊的处理。</p>
<p>由于t_{[CLS]}蕴含了所有可推理出的事实，而我们希望t_{NAF}可以编码所有包含否定的事实。对于一个包含否定的事实，如果它的反面是不能由语境推理出来的，那么任务它本身是True（见Q4的推理中"The bald eagle is not kind"，它的反面"The bald eagle is kind"是不能推理得到的，因此它是True）。</p>
<p>我们希望将t_{NAF}表示为<span class="math inline">\(t_{[CLS]}\)</span>（所有可以推理出的事实）的一个函数，来表示所有不能被推理出的事实的反面。虽然一个证明中可能有很多个NAF，但是我们将他们放在一起，学习一个统一的表示。</p>
<p>我们用以下公式计算节点的概率： <span class="math display">\[
P_{Node}=\sigma(H_{Node}(\{t_{RF_i}\}_{i=1}^k, t_{NAF}))
\]</span> 其中<span class="math inline">\(H_{Node}\)</span>结构和<span class="math inline">\(H_{QA}\)</span>类似。</p>
<h2 id="边模块">边模块</h2>
<p>在我们已经生成了所有事实、规则和NAF的表示之后，我们来学习它们之间的边的表示。</p>
<p>我们定义由节点<span class="math inline">\(RF_i\)</span>到<span class="math inline">\(RF_j\)</span>的边的表示为 <span class="math display">\[
t_{(RF_i,RF_j)} = [t_{RF_i},t_{RF_j},(t_{RF_i}-t_{RF_j})]
\]</span> （这里的减法是逐分量减，表征方向）</p>
<p>然后用以下公式计算边的概率 <span class="math display">\[
P_{Edge}=\sigma(H_{Edge}(\{t_{(RF_i,RF_j)}\}_{i,j=1}^{k+1})) \\
其中t_{RF_{k+1}} = t_{NAF}
\]</span></p>
<h2 id="loss">LOSS</h2>
<p>三部分都用二元交叉熵loss，相加得到总loss <span class="math display">\[
L = L_{QA} + L_{Node} + L_{Edge}
\]</span></p>
<h2 id="整数线性规划作为全局约束">整数线性规划作为全局约束</h2>
<h3 id="证明连通性">证明连通性</h3>
<p>验证<span class="math inline">\(\mathcal P\)</span>是否联通，作者构造一个扩充图<span class="math inline">\(\mathcal P_{aug} = (\mathcal N_{aug}, \mathcal E_{aug})\)</span>，添加一个源点和一个汇点，由源向原图中的每个节点都引一条边，再从原图中的每个节点向汇引一条边。</p>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204141636739.png" class>
<p>然后我们就可以在<span class="math inline">\(\mathcal P_{aug}\)</span>上跑最大流了（不会吧不会吧，不会有人把离散2忘干净了吧？）</p>
<h3 id="最终的优化问题">最终的优化问题</h3>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204142231185.png" class>
<p>其中<span class="math inline">\(\phi(m,n)\)</span>表示<span class="math inline">\(m\to n\)</span>的边存在的概率（由PRover预测）</p>
<p>解这个带约束最优化问题就可以解出我们的证明啦！（这也太……玄妙了）</p>
<h1 id="实验">实验</h1>
<h2 id="数据集和指标">数据集和指标</h2>
<ul>
<li>DU0~DU5</li>
<li>Birds-Electricity</li>
<li>ParaRules</li>
</ul>
<p>都来自<a href="https://zcy.moe/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/">这篇文章</a>。</p>
<p>衡量指标包括：</p>
<ul>
<li><p>节点准确度</p>
<p>预测的证明中节点集与实际的节点集的匹配程度</p></li>
<li><p>边准确度</p>
<p>预测的证明中边集与实际的边集的匹配程度</p></li>
<li><p>证明准确度</p>
<p>预测的证明与实际证明的匹配程度</p></li>
<li><p>完整准确度</p>
<p>答案和证明都和标答完全一致</p></li>
</ul>
<h2 id="不同深度下的qa和证明准确度">不同深度下的QA和证明准确度</h2>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204143338820.png" class>
<p>QA整体上最简单，然后是NA，再次是EA和PA</p>
<h2 id="zero-shot评估">zero-shot评估</h2>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204143345257.png" class>
<h2 id="向更深的问题泛化">向更深的问题泛化</h2>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204144527505.png" class>
<h2 id="改变训练数据规模">改变训练数据规模</h2>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204144338113.png" class>
<h2 id="在更复杂的语言上评估">在更复杂的语言上评估</h2>
<img data-src="/2021/02/04/PROVER-Proof-Generation-for-Interpretable-Reasoning-over-Rules%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210204144316610.png" class>
<p>​</p>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>ProofWriter: Generating Implications, Proofs, and Abductive Statements over Natural Language阅读笔记</title>
    <url>/2021/02/03/ProofWriter-Generating-Implications-Proofs-and-Abductive-Statements-over-Natural-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="https://arxiv.org/pdf/2012.13048.pdf">论文链接</a></p>
<p>废话：</p>
<p>我感觉这就是拿着T5做了个finetune……它的迭代式模型很有意思，也很暴力。刚看到一次性模型和迭代式模型的表现差不多的时候我还挺失望的，后来发现迭代式模型在泛化能力上的优势还是有的。</p>
<p>感觉是不是可以怎么优化一下迭代式模型的迭代过程，不要没头苍蝇式的地乱撞。不行可以backward chaining？</p>
<h1 id="摘要">摘要</h1>
<p>作者提出了一个叫做ProofWriter的生成式模型，既可以用自生成理论的含义，又可以写出用自然语言表示的证明过程。</p>
<a id="more"></a>
<h1 id="引入">引入</h1>
<p>已有的自然语言进行逻辑推理的模型仅限于对给定标签的真假判定，而不能基于事实去推理出新的性质。</p>
<p>作者提出了ProofWriter模型，每次都会进行一次单步的推理并且生成简单的证明，然后将推理的结果加入到语境中以供下一次继续推理，并且基于这些碎片的单步推理构建完整的证明。</p>
<img data-src="/2021/02/03/ProofWriter-Generating-Implications-Proofs-and-Abductive-Statements-over-Natural-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210202225714440.png" class>
<p>单步推理的可信度是极高的，而且证明过程中每一步决策都可以清晰地看出（比起那些事后再分析模型决策的方法要更可靠）。而且这样的模型的效果比一步到底生成完整证明的模型效果好。</p>
<p>生成方法还有一些全新的应用：首先，ProofWriter生成的含义是自然语言的，从而允许枚举许多结果，而不是单纯给推理之前就定好的假设一个一个赋针值。其次，ProofWriter还可以生成诱导性的陈述，即给定一个理论和一个无法证明的结论，ProofWriter会尝试找到那个缺失的条件（如果存在的话）而且给出添加这个条件之后的证明。</p>
<h1 id="相关工作">相关工作</h1>
<ul>
<li><p><a href="https://arxiv.org/pdf/2010.02830.pdf">PRover</a></p>
<p>第一个尝试去做证明生成的，但是效果并不好，上文中已经被作者鞭尸好几次了</p></li>
<li><p><a href="https://arxiv.org/pdf/2009.14786.pdf">Measuring Systematic Generalization in Neural Proof Generation with Transformers</a></p>
<p>也尝试了做证明的生成，但是它是从许多例子里去得到一些【已经给定的】规则，然后用于推理，和本文的路子不同</p></li>
</ul>
<h1 id="实现方法">实现方法</h1>
<p>第一次看到这么事无巨细的论文作者……对于初学者而言简直是天使</p>
<h2 id="名词定义">名词定义</h2>
<ul>
<li><span class="math inline">\(C\)</span>表示<span class="math inline">\(theory\)</span>，是由fact <span class="math inline">\(F\)</span>和rule <span class="math inline">\(R\)</span>组成的英文句子集合。我们也将<span class="math inline">\(C\)</span>叫做<span class="math inline">\(context\)</span>
<ul>
<li>笔记里提到的理论和语境都是<span class="math inline">\(C\)</span></li>
</ul></li>
<li><span class="math inline">\(Q\)</span>表示<span class="math inline">\(Question\)</span>，是一个英文句子表示的假设，其真值可以单纯由<span class="math inline">\(C\)</span>中的信息确定。
<ul>
<li>笔记里提到的问题<strong>可能</strong>是<span class="math inline">\(Q\)</span></li>
</ul></li>
<li><span class="math inline">\(A\)</span>表示<span class="math inline">\(answer\)</span>，在封闭世界假设中<span class="math inline">\(A\in\{True, False\}\)</span>，在开放世界假设中 <span class="math inline">\(A\in\{True, False, Unknown\}\)</span></li>
<li><span class="math inline">\(P\)</span>表示<span class="math inline">\(proof\)</span></li>
<li><span class="math inline">\(I\)</span>表示<span class="math inline">\(implication\)</span>，一个可以由<span class="math inline">\(C\)</span>推导出的事实。
<ul>
<li>笔记中提到的含义都是<span class="math inline">\(I\)</span></li>
</ul></li>
</ul>
<p>【个人加的一个】笔记中提到的“事实”对应英文fact，表示一个陈述，不代表它的真值是<span class="math inline">\(True\)</span>或<span class="math inline">\(False\)</span>。</p>
<p>作者定义了三种任务：</p>
<ul>
<li><p><span class="math inline">\(\textbf{proof(question-answering)}\)</span></p>
<p>给定<span class="math inline">\(C\)</span>和假定的事实<span class="math inline">\(Q\)</span>，求<span class="math inline">\(A\)</span>和<span class="math inline">\(P\)</span>（如果存在）</p></li>
<li><p><span class="math inline">\(\textbf{enumeration}\)</span></p>
<p>给定<span class="math inline">\(C\)</span>，生成<span class="math inline">\(I\)</span>，越多越好</p></li>
<li><p><span class="math inline">\(\textbf{abduction}\)</span></p>
<p>给定<span class="math inline">\(C\)</span>和期望证明的事实<span class="math inline">\(Q\)</span>，寻找辅助事实<span class="math inline">\(f_m\)</span>，使得<span class="math inline">\(C\)</span>在添加上<span class="math inline">\(f_m\)</span>后可以证明出<span class="math inline">\(Q\)</span></p></li>
</ul>
<h2 id="语义">语义</h2>
<p>一个事实为<span class="math inline">\(True\)</span>，要么它已知，要么它可以表示为为<span class="math inline">\(True\)</span>的规则推理出的结论。</p>
<p>对于<span class="math inline">\(False\)</span>，作者采取了两种不同的假设：</p>
<ul>
<li>封闭世界假设(Closed World Assumption)采取否定即假（Negation As Failure）原则，只允许<span class="math inline">\(True\)</span>和<span class="math inline">\(False\)</span>出现，不能被证明为<span class="math inline">\(True\)</span>的都是<span class="math inline">\(False\)</span></li>
<li>开放世界假设(Open World Assumption)则允许第三种真值<span class="math inline">\(Unknown\)</span>的出现，不能被证明为<span class="math inline">\(True\)</span>或<span class="math inline">\(False\)</span>的事实被认为是<span class="math inline">\(Unknown\)</span></li>
</ul>
<p>两种不同的假设作者都做了实验，分别采用不同的数据集。</p>
<h2 id="证明的表示">证明的表示</h2>
<p>定义对事实<span class="math inline">\(f_q\)</span>的证明<span class="math inline">\(P\)</span>是一个有向无环图<span class="math inline">\((N,E)\)</span>。</p>
<p><span class="math inline">\(P\)</span>中的每一个节点都要么是事实<span class="math inline">\(f\)</span>（基本字面量）或者规则<span class="math inline">\(r\)</span>（逻辑表达式），都是由英语表达的。</p>
<p>P中的每一条边要么将一个事实连接到规则上，表示这个事实满足了规则的条件；要么将一个规则连接到事实上，表明这个事实是由规则推理出来的。因此，证明树上的每根树枝上事实和规则都会交替出现。</p>
<p>证明中的事实分三种：已知事实<span class="math inline">\(f_i\in F\)</span>，否定事实<span class="math inline">\(f_{naf}\)</span>（依据否定即假的原则，一定是假的）和结论事实<span class="math inline">\(f_{conc}\)</span>（由规则推理出的事实）。依据定义，否定事实和结论事实都不出现在<span class="math inline">\(F\)</span>中。</p>
<img data-src="/2021/02/03/ProofWriter-Generating-Implications-Proofs-and-Abductive-Statements-over-Natural-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210202231338762.png" class>
<div data-align="center">
三类fact的一个例子，naf1好像是凭空变出来的？
</div>
<h2 id="证明的编码">证明的编码</h2>
<p>出现在语境里的每个事实和规则都有编号，fact1, fact2, ..., rule1, rule2, ...</p>
<p>naf和conc的fact也会有编号，naf1, naf2, ..., conc1, conc2,...，但因为它们并没有出现在语境里，因此要额外用以下的形式来表示："with conc1: xxx. conc2: xxx ..."</p>
<p>&amp;表示合取<span class="math inline">\(\wedge\)</span>，%表示<span class="math inline">\(\to\)</span>，#表示<span class="math inline">\(\leftarrow\)</span></p>
<h2 id="模型">模型</h2>
<p>用了预训练Transformer T5，用了最大的T5-11B模型。默认超参数，Adafactor优化器，40000步，batch size为8，用validation score最高的那个checkpoint（通常情况下是最后一个）。</p>
<p>三种不同任务有不同输入输出格式。</p>
<h3 id="本领一生成证明">本领一：生成证明</h3>
<p>作者采用了两种方法来生成：</p>
<ul>
<li><p>一次性</p>
<p>训练模型输入理论和问题，一次性生成完整证明和答案</p></li>
<li><p>迭代式</p>
<p>训练模型输入理论和问题，每次输出一个单步证明</p>
<p>不断迭代直到推不出更多结果为止</p>
<p>然后用单步证明组装完整证明</p></li>
</ul>
<h3 id="本领二含义枚举">本领二：含义枚举</h3>
<p>给定C，预测I</p>
<h3 id="本领三单事实诱导证明">本领三：（单事实）诱导证明</h3>
<p>这个只在开放世界假设上做。</p>
<p>【这部分没看懂】</p>
<h1 id="数据集">数据集</h1>
<p>采用数据集<span class="math inline">\(RuleTaker D^*\)</span>（包括D0、D1、D2、D3和D5），针对封闭世界假设和开放世界假设分别做了两个变种，包含原版在内的三种数据集被称为<span class="math inline">\(D^*(\text{orig}), D^*(\text{CWA}), D^*(\text{OWA})\)</span>。</p>
<p>Birds-Electricity数据集规模小、来自真实世界，由人类手写，用来衡量训练数据以外的模型表现。</p>
<p>ParaRules数据集测试将知识转化为更自然的语言的能力。</p>
<h1 id="结果">结果</h1>
<h2 id="本领一生成证明-1">本领一：生成证明</h2>
<h3 id="与以往工作相比">与以往工作相比</h3>
<h4 id="in-domain">in-domain</h4>
<p>一次性和迭代式的结果几乎完全相同。</p>
<p>生成答案的准确度和PRover一致，正确证明的数目明显更多。</p>
<h4 id="out-of-domain">Out of Domain</h4>
<p>一次性和迭代式都强于PRover</p>
<p>迭代式模型的鲁棒性明显更好。</p>
<h3 id="一次性和迭代式的比较">一次性和迭代式的比较</h3>
<p>准确度表现差不多</p>
<p>迭代式模型在较长证明的泛化能力上明显强于一次性模型，也有更好的鲁棒性</p>
<h3 id="验证一次性模型生成的证明">验证一次性模型生成的证明</h3>
<p>直接针对证明的中间结果向模型提问即可，提供了更好的可解释性和可验证性。</p>
<h2 id="本领二含义枚举-1">本领二：含义枚举</h2>
<p>迭代式模型效果更好。</p>
<h2 id="本领三单事实诱导证明-1">本领三：（单事实）诱导证明</h2>
<p>预测的factM和真实的factM匹配度挺高的</p>
<p>在D3-Ab上训练的模型，在D3和D5上测试的结果高度一致，这可能暗示D5-Ab实际上不是out-of-domain的（虽然D5的问题都是比D3的推理步骤更深，但是它们都是可证明的，走一个事实换成不可证明的之后可能其分布与D3-Ab没有本质区别）。</p>
<h1 id="讨论">讨论</h1>
<h2 id="一次性vs迭代式">一次性vs迭代式</h2>
<p>一次性模型短平快且有效，但迭代式的泛化能力更好，尤其是在更深的推理上。</p>
<p>尽管单步推理的误差会积累，但是由于单步推理精度实在太高，在较深的推理方面叠加之后效果仍然比一次性好。</p>
<p>迭代式也有缺点：首先它需要生成所有可能的知识才能做推理，效率很低。其次，在推理出的中间结果过多后，它可能会突破512token的限制，爆模型……</p>
<h2 id="隐式知识">隐式知识</h2>
<p>很多模型需要所有应用的规则都被显式表征出来。</p>
<p>ProofWriter可以利用自己的诱导能力首先找到那个缺失的事实，再去进行证明。</p>
<p>这样就可以更好地理解隐式知识在推理中的应用。</p>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>Rust泛型简单笔记</title>
    <url>/2021/01/26/Rust%E6%B3%9B%E5%9E%8B%E7%AE%80%E5%8D%95%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="泛型基本语法">泛型基本语法</h2>
<h3 id="函数中的泛型">函数中的泛型</h3>
<p>回忆一下函数的定义</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">function_name</span></span>(params: types) -&gt; return_type &#123;</span><br><span class="line">    <span class="comment">//function_body</span></span><br><span class="line">    return_value</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在函数中使用泛型，只需要在函数名后面加上尖括号，里面包裹类型名就可以了</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">function_name</span></span>&lt;T&gt;(params: types) -&gt; return_type &#123;</span><br><span class="line">    <span class="comment">//function_body</span></span><br><span class="line">    return_value</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<a id="more"></a>
<h3 id="结构体中的泛型">结构体中的泛型</h3>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">PointI32</span></span> &#123;</span><br><span class="line">    x: <span class="built_in">i32</span>,</span><br><span class="line">    y: <span class="built_in">i32</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在类型后面加上尖括号，里面包裹类型名</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Point</span></span>&lt;T&gt; &#123;</span><br><span class="line">    x: T,</span><br><span class="line">    y: T,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>注意当用于构造的值不是同一类型时，（无论是否显式指定类型）rust不会进行类型转换而是会报错。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> a: <span class="built_in">i32</span> = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">let</span> b: <span class="built_in">isize</span> = <span class="number">20</span>;</span><br><span class="line"><span class="keyword">let</span> p1 = Point &#123;</span><br><span class="line">    x: a,</span><br><span class="line">    y: b,</span><br><span class="line">&#125; <span class="comment">//报错</span></span><br><span class="line"><span class="keyword">let</span> p2 = Point::&lt;<span class="built_in">i32</span>&gt; &#123;</span><br><span class="line">    x: a,</span><br><span class="line">    y: b,</span><br><span class="line">&#125; <span class="comment">//仍然报错</span></span><br></pre></td></tr></table></figure>
<h3 id="枚举定义中的泛型">枚举定义中的泛型</h3>
<p>和结构体类似，直接上两个喜闻乐见的例子</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">Option</span></span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="literal">Some</span>(T),</span><br><span class="line">    <span class="literal">None</span>,</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">Result</span></span>&lt;T, E&gt; &#123;</span><br><span class="line">    <span class="literal">Ok</span>(T),</span><br><span class="line">    <span class="literal">Err</span>(E),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="方法定义中的泛型">方法定义中的泛型</h3>
<p>和函数类似，在<code>impl</code>后面也要加泛型参数</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">impl</span>&lt;T&gt; Point&lt;T&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">x</span></span>(&amp;<span class="keyword">self</span>) -&gt; &amp;T &#123;</span><br><span class="line">        &amp;<span class="keyword">self</span>.x</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>也可以模板特化</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">impl</span> Point&lt;<span class="built_in">f32</span>&gt; &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">distance</span></span>(&amp;<span class="keyword">self</span>) -&gt; <span class="built_in">f32</span> &#123;</span><br><span class="line">        (<span class="keyword">self</span>.x.powi(<span class="number">2</span>) + <span class="keyword">self</span>.y.powi(<span class="number">2</span>)).sqrt()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="trait">Trait</h2>
<p>个人的理解是提供不同类型的相同接口，也就是类似有继承的语言中虚函数的作用。</p>
<p>官方的教程中将它解释为一个类似<code>interface</code>（当然继承虚基类也就是提供统一interface）的东西。</p>
<p>例如</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Animal</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">behavior</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Cat</span> :</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">behavior</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">&quot;Meow!&quot;</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dog</span> :</span> <span class="keyword">public</span> Animal &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">behavior</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">&quot;Woof!&quot;</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Animal*&gt; vec&#123;<span class="keyword">new</span> Cat(), <span class="keyword">new</span> Dog()&#125;;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> each : vec) &#123;</span><br><span class="line">        each-&gt;behavior();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在rust中可以写成</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Cat</span></span> &#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Dog</span></span> &#123;&#125;</span><br><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">Behavior</span></span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">behavior</span></span>(&amp;<span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">impl</span> Behavior <span class="keyword">for</span> Cat &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">behavior</span></span>(&amp;<span class="keyword">self</span>) &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;Meow!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">impl</span> Behavior <span class="keyword">for</span> Dog &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">behavior</span></span>(&amp;<span class="keyword">self</span>) &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;Wooff!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">Animal</span></span> &#123;</span><br><span class="line">    Cat(Cat),</span><br><span class="line">    Dog(Dog),</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">main</span></span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> animals = <span class="built_in">vec!</span>[Animal::Cat(Cat&#123;&#125;), Animal::Dog(Dog&#123;&#125;)];</span><br><span class="line">    <span class="keyword">for</span> animal <span class="keyword">in</span> animals.iter() &#123; <span class="comment">//假定我们实现了IntoIterator...</span></span><br><span class="line">        <span class="keyword">match</span> animal &#123;</span><br><span class="line">            Animal::Cat(cat) =&gt; cat.behavior();</span><br><span class="line">            Animal::Dog(dog) =&gt; dog.behavior();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="定义trait">定义trait</h3>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">trait</span> <span class="title">TraitName</span></span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">SharedBehavior</span></span>(&amp;<span class="keyword">self</span>) -&gt; return_type; <span class="comment">//这里也可以实现，作为trait的默认实现。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="实现trait">实现trait</h3>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">impl</span> TraitName <span class="keyword">for</span> StructName &#123;</span><br><span class="line">    <span class="function"><span class="keyword">fn</span> <span class="title">SharedBehavior</span></span>(&amp;<span class="keyword">self</span>) -&gt; return_type &#123;</span><br><span class="line">        <span class="comment">//function_body</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="在泛型中指定trait">在泛型中指定trait</h3>
<p>有时候我们对泛型参数有一些要求，比如【可以比大小】：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">GetLargest</span></span>&lt;T&gt;(list: &amp;[T]) -&gt; T &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> largest = list[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span> &amp;each <span class="keyword">in</span> list.iter() &#123;</span><br><span class="line">        <span class="keyword">if</span> each &gt; largest &#123; <span class="comment">//有问题，这里需要假定它可以比大小</span></span><br><span class="line">            largest = each;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    largest</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>为了给泛型参数加上更强的条件，我们给类型加上Trait Bound。如果需要多个Trait Bound，用+连接即可</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">GetLargest</span></span>&lt;T: <span class="built_in">PartialOrd</span> + <span class="built_in">Copy</span>&gt;(list: &amp;[T]) -&gt; T</span><br></pre></td></tr></table></figure>
<p>或者这样</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">GetLargest</span></span>&lt;T&gt;(list: &amp;T) -&gt; T</span><br><span class="line">    <span class="keyword">where</span> T: <span class="built_in">PartialOrd</span> + <span class="built_in">Copy</span></span><br></pre></td></tr></table></figure>
<p>因此上面的函数可以写成这样</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">GetLargest</span></span>&lt;T: <span class="built_in">PartialOrd</span> + <span class="built_in">Copy</span>&gt;(list: &amp;[T]) -&gt; T &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> largest = list[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span> &amp;each <span class="keyword">in</span> list.iter() &#123;</span><br><span class="line">        <span class="keyword">if</span> each &gt; largest &#123;</span><br><span class="line">            largest = each;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    largest</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>或者如果不要copy，返回引用，也可以这样</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">GetLargest</span></span>&lt;T: <span class="built_in">PartialOrd</span>&gt;(list: &amp;[T]) -&gt; &amp;T &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut</span> largest = &amp;list[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">for</span> each <span class="keyword">in</span> list.iter() &#123;</span><br><span class="line">        <span class="keyword">if</span> each &gt; largest &#123;</span><br><span class="line">            largest = each;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    largest</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="返回实现了指定trait-bound的函数">返回实现了指定Trait Bound的函数</h3>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">fn</span> <span class="title">funcion_name</span></span>(params: types) -&gt; <span class="keyword">impl</span> Trait_name</span><br></pre></td></tr></table></figure>
<p>但这样只是依据返回值去自动推断类型，因此如果要</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (case) &#123;</span><br><span class="line">    <span class="keyword">return</span> Cat&#123;&#125;;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> Dog&#123;&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那还是不可以的。比较语法糖。</p>
<h3 id="用trait-bound有条件地实现方法">用Trait Bound有条件地实现方法</h3>
<p>和函数没啥区别，在<code>impl</code>后面的类型里指定trait bound就可以了。</p>
]]></content>
      <categories>
        <category>编程语言</category>
      </categories>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Transformers as Soft Reasoners over Language阅读笔记</title>
    <url>/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p><a href="Transformers%20as%20Soft%20Reasoners%20over%20Language">论文链接</a></p>
<p>单纯觉得这个灯泡表情非常魔性……</p>
<img data-src="/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210203153124905.png" class title="我要一张色图">
<h1 id="摘要">摘要</h1>
<p>作者提出了名叫RuleTaker的推理模型，其中的事实和规则都是自然语言（而不需要逻辑表示或者概率表示这种规范表示），以Transformer为基础在自然语言上进行推理。</p>
<p>值得一提的是，作者本人也不很信任这个推理，所以也叫它软推理……</p>
<p>这一模型在两个人工创作的规则集上有较好的泛化表现。</p>
<a id="more"></a>
<h1 id="引入">引入</h1>
<p>以往的推理模型一般需要将规则和事实表示为规范形式，比如概率表示、逻辑表示。作者探索一条全新的路径：规则和事实都表示为自然语言，尝试利用transformer在自然语言上进行推理。</p>
<p>与已有的许多任务不同，这里作者的任务是显式给定规则和事实，希望让transformer给出正确的结论。这张图片可以作为一个例子：</p>
<img data-src="/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210203152837990.png" class>
<p>作者的结论是在某种程度上Transformer可以用于推理，可以作为所谓的“soft theorem prover”。</p>
<p>作者的研究存在一下假定和限制：</p>
<ul>
<li>规则是若干自然语言表示的<span class="math inline">\(condition\)</span>（下文均称为条件）的合取</li>
<li>语义与logic programs with negation相同</li>
<li>推理过程就是依据条件给出结论真值的过程</li>
</ul>
<p><span class="math display">\[
[\wedge condition]^*\to conclusion
\]</span></p>
<p><a name="bulb"></a></p>
<img data-src="/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210203154207949.png" class>
<p>至于Transformer是不是真的在“推理”，作者也不知道。。。</p>
<p>几个问答：</p>
<ul>
<li><p><strong>Transformer可以基于规则来推理吗？</strong></p>
<p>作者在合成的语言（不是人工创作的真自然语言）上训练和测试，得到了较好的结果。</p></li>
<li><p><strong>训练出来的模型可以解决人工创作的推理问题吗？</strong></p>
<p>zero-shot下可以达到90+的准确度。</p></li>
<li><p><strong>这个结果可以在更自然的语言上继续保持吗？</strong></p>
<p>对于众包出来的自然语言效果也不错。</p></li>
<li><p><strong>模型可以找出答案来自哪些事实吗？</strong></p>
<p>达到了94%的F1 score，对超过70%的问题都完美找到了，因此作者认为可以。</p></li>
<li><p><strong>其他架构也可以推理吗？</strong></p>
<p>作者用的是RoBERTa，BERT和ESIM（基于LSTM的一个模型）也可以，但是效果要差一点。</p>
<p>这说明结果不限定于RoBERTa，甚至不限定于Transformer，但Transformer表现要更好一点。</p></li>
</ul>
<h1 id="数据集生成">数据集生成</h1>
<p>作者生成了不同推理深度的若干不同数据集，但格式相同。</p>
<p>数据集中的每个例子都是一个三元组<span class="math inline">\((context, statement, answer)\)</span>，其中<span class="math inline">\(context\)</span>形如<span class="math inline">\((fact^*, rule^*)\)</span>；<span class="math inline">\(statement\)</span>是问题——也即一个要验证真假的陈述句；<span class="math inline">\(answer\)</span>是<span class="math inline">\(True\)</span>或<span class="math inline">\(False\)</span>（此处依照封闭世界假设，如果<span class="math inline">\(statement\)</span>能被<span class="math inline">\(context\)</span>推理出来，那就是<span class="math inline">\(True\)</span>，否则都是<span class="math inline">\(False\)</span>，不存在<span class="math inline">\(Unknown\)</span>这种类型）。</p>
<h2 id="综述">综述</h2>
<p>要生成例子，首先生成一些小规模的理论（事实+规则），然后做前向推理得到一些含义（结论），然后从这些结论里选出一些作为<span class="math inline">\(statement\)</span>（答案为True）、再从那些无法证明的事实里选出一些作为<span class="math inline">\(statement\)</span>（在CWA下，答案为False）。</p>
<p>所有的事实、规则和问题都是用简单的自然语言模板通过机器合成的英语。依照推理深度<span class="math inline">\(D\)</span>不同（<span class="math inline">\(D=0, D\leq 1, D\leq 2, D\leq 3, D\leq 5\)</span>）分别构建了<span class="math inline">\(D0, D1, D2, D3, D5\)</span>五个数据集（<span class="math inline">\(D0\)</span>就是不需要推理直接找答案）。</p>
<p>前四个用于训练，最后一个用来测试泛化能力。</p>
<h2 id="理论生成">理论生成</h2>
<p>理论包含了两种事实</p>
<ul>
<li>属性 <span class="math inline">\(is(e_i, a_j)\)</span></li>
<li>关系<span class="math inline">\(r_k(e_i, e_k)\)</span></li>
</ul>
<p><span class="math inline">\(is\)</span>用于将属性赋予实体，而<span class="math inline">\(r_k\)</span>用于将实体联系起来。</p>
<p>理论具有如下的形式 <span class="math display">\[
condition[\wedge condition]^*\to conclusion
\]</span> 第一个<span class="math inline">\(condition\)</span>是一个谓词，它的第一个参数是变量，第二个参数是属性或实体。</p>
<p>后面的<span class="math inline">\(condition\)</span>也是谓词，它的第一个参数要么是与前面相同的变量，要么是前面提到过的实体；第二个参数的新的属性或者新的实体。</p>
<p>（这种情况下，规则最多含有一个变量）</p>
<p>举一个例子，<code>If someone is young and round then they are kind</code>的标准形式是<span class="math inline">\(is(?X, Young)\,\wedge\,is(?X, Round)\to is(?X, Kind)\)</span>。</p>
<p>作者随机生成的理论有两种：</p>
<ul>
<li>第一种只有<span class="math inline">\(is\)</span>谓词，从10个名字和14个属性里随机抽取4个实体和7个属性。</li>
<li>第二种有<span class="math inline">\(is\)</span>和其他3个谓词（从6个谓词中随机抽取），从10个名字和10个属性里随机抽取4个实体和5个属性。</li>
</ul>
<h2 id="前向推理">前向推理</h2>
<p>对于给定的理论，作者穷举它能找到的结论（领域有限，因此结论也有限）。</p>
<h2 id="问题生成和英语合成">问题生成和英语合成</h2>
<p>对于每个理论，作者找出各推理深度的结论各一个（比例，在<span class="math inline">\(D2\)</span>数据集中，一个结论会找<span class="math inline">\(D=0, 1,2\)</span>的结论各一个作为问题）作为问题（答案为True）。</p>
<p>对于每个True问题，作者通过将它的结论取否定来构造一个False问题。</p>
<p>然后再找相同数目的无法证明的事实（CWA下这就等同于False），把其中一半的事实取否定再把真值改成True。这样最终True和False问题各一半。 <span class="math display">\[
context: \\
小明喜欢吃苹果\\
小亮喜欢吃香蕉\\
如果一个人喜欢吃苹果，那么他很可爱\\
statement:\\
小明很可爱(True) \\
小明不可爱(False) \\
小亮很可爱(False，因为不可证明(也不可证伪)) \\
小亮不可爱(同样是False，因为不可证明(也不可证伪)\\
\neg \,小亮很可爱(True，因为小亮很可爱是False)
\]</span> 最后用简单的If-Then英语模板生成合成的语言。</p>
<h1 id="实验">实验</h1>
<h2 id="模型">模型</h2>
<p>用了 <span class="math inline">\(RoBERTa\)</span>，首先在RACE数据集上finetune过，超参数也和RACE上finetune的RoBERTa相一致。</p>
<p>输入格式如下： <span class="math display">\[
[CLS]\,context\,[SEP]\,statement\,[SEP]
\]</span> 输出取对数，大于0就是真，小于0就是假。</p>
<p>loss用交叉熵。</p>
<h2 id="roberta能回答推理问题吗">RoBERTa能回答推理问题吗？</h2>
<img data-src="/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210203172348122.png" class>
<ul>
<li>可以，超过99%的准确度表明了这一点</li>
<li>D0上训练的模型完全不能用于推理</li>
<li>D1、D2上训练的模型泛化能力不好，但D3上训练的模型在D5上泛化得很不错</li>
</ul>
<p>为了测试模型抵御干扰的能力，他们又做了一些扰动：</p>
<p>对于每个True的结论，他们尝试删除证明过程中用到的一个句子（从而试图把真值从True变成False），看模型是否能做出正确的inference。</p>
<ul>
<li>总体准确度几乎不变</li>
<li>对于那些希望把真值翻转的20000个问题，有81%的真值被翻转了，所以说模型具有一定的鲁棒性</li>
</ul>
<h2 id="人工生成的数据集上的表现">人工生成的数据集上的表现</h2>
<p>用到了Bird数据集和Electricity数据集</p>
<img data-src="/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210203173334675.png" class>
<ul>
<li>除了D0上训练的模型以外，几乎所有的模型都完美解决了Bird问题</li>
<li>D5上训练的模型在（除了一个以外的）所有数据集上都得到了90%+的分数</li>
</ul>
<h2 id="用人工转译的规则做推理">用人工转译的规则做推理</h2>
<img data-src="/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210203173104649.png" class>
<p>人工转译后fact和rule都变得更复杂了，新的数据集叫做ParaRules。注意ParaRules里只包含第一类理论（只有is谓词的那个）</p>
<p>没有在ParaRules上训练过的模型做zero-shot推理，效果尚可。在D3和ParaRules上训练过的模型效果就很好了。</p>
<img data-src="/2021/02/03/Transformers-as-Soft-Reasoners-over-Language%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/image-20210203173319013.png" class>
<h2 id="改用其他模型架构">改用其他模型架构</h2>
<p>为了探究模型在多大程度上依赖RoBERTa架构，作者又尝试了其他模型，包括原始BERT、基于LSTM的ESIM模型和一个可分解的注意力模型DECOMP（用于证明这个数据集并不trivial）。</p>
<p>最好的BERT在D5上准确度超过了95%，而ESIM只有约80%。对照组DECOMP只有57.4%，说明模型并不平凡可解。</p>
<p>为了说明预训练的用处，作者采取一个策略，从每个event中将一个词随机替换从而使得句子毫无语法可言。在这种情况下，RoBERTa的准确度显著下降了，这说明预训练带来的语言知识使得学习变得更加容易</p>
<ul>
<li>预训练比非预训练模型更好</li>
<li>结论不只依赖Transformer，但Transformer会比其他模型表现更好</li>
</ul>
<h1 id="未来的发展">未来的发展</h1>
<ul>
<li>扩大理论的规模</li>
<li>更全面地生成训练数据</li>
<li>规范的定理证明</li>
<li>自然语言推理</li>
</ul>
]]></content>
      <categories>
        <category>炼丹</category>
      </categories>
  </entry>
  <entry>
    <title>更换hexo的渲染引擎</title>
    <url>/2021/02/01/%E6%9B%B4%E6%8D%A2hexo%E7%9A%84%E6%B8%B2%E6%9F%93%E5%BC%95%E6%93%8E/</url>
    <content><![CDATA[<p>今天写论文笔记的时候，发现hexo对mathjax渲染的时候Markdown和<span class="math inline">\(\LaTeX\)</span>会产生冲突，主要是<code>_</code>和<code>*</code>这两个字符。之前因为问题加个<code>\</code>就解决了，所以也没有在意。但是今天这个冲突比较厉害，一时间改不过来，去查找解决方案的时候发现可以把hexo的渲染引擎改为pandoc。</p>
<a id="more"></a>
<p>首先检查有没有安装pandoc <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt install pandoc</span><br></pre></td></tr></table></figure> 发现pandoc已经安装</p>
<p>然后卸载默认渲染器，安装pandoc渲染插件</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm uninstall hexo-renderer-marked --save</span><br><span class="line">npm install hexo-renderer-pandoc --save</span><br></pre></td></tr></table></figure>
<p>当我满心欢喜地<code>hexo g</code>的时候，却发现报了错误<code>pandoc: Unknown extension: smart</code></p>
<p>查过之后发现问题在于pandoc版本太低，需要改为2以上。 可是apt明明告诉我版本是最新的啊？ <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pandoc --version</span><br></pre></td></tr></table></figure> 发现不但是1.19.2.4，而且还是i386……</p>
<p>于是到官网上去下载，官网上也有现成的针对各大distro的安装包 <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">wget https:&#x2F;&#x2F;github.com&#x2F;jgm&#x2F;pandoc&#x2F;releases&#x2F;download&#x2F;2.11.4&#x2F;pandoc-2.11.4-1-amd64.deb</span><br><span class="line">sudo dpkg -i pandoc-2.11.4-1-amd64.deb</span><br></pre></td></tr></table></figure> 然后再<code>hexo g</code>就可以啦！</p>
]]></content>
      <categories>
        <category>博客</category>
      </categories>
  </entry>
  <entry>
    <title>更换hexo的评论系统</title>
    <url>/2021/02/05/%E6%9B%B4%E6%8D%A2hexo%E7%9A%84%E8%AF%84%E8%AE%BA%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<p>在刚搭起博客的时候，我用的评论系统是<a href="https://disqus.com/">Disqus</a>，大概长这个样子</p>
<img data-src="/2021/02/05/%E6%9B%B4%E6%8D%A2hexo%E7%9A%84%E8%AF%84%E8%AE%BA%E7%B3%BB%E7%BB%9F/image-20210205174037408.png" class>
<p>Disqus在世界范围内的用户很多，也比较有名。但问题也是显而易见的：需要登录社交账号来留言，太笨重；而且如果不科学上网，Disqus根本无法使用。</p>
<p>因此，我希望能找到一个墙内可用、不需要登录的轻量的评论系统，然后我从<a href="blog.panda2134.site">同学的blog</a>那里看到了<a href="https://valine.js.org/">valine</a>。</p>
<p>valine的使用需要基于BaaS平台leanCloud，依据<a href="https://valine.js.org/quickstart.html">快速开始页</a>进行设置就好。</p>
<p>然后在<code>/themes/next/_config.yml</code>里配置：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">comments:</span></span><br><span class="line">  <span class="attr">active:</span> <span class="string">valine</span></span><br></pre></td></tr></table></figure>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">valine:</span></span><br><span class="line">  <span class="attr">enable:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">appid:</span> [<span class="string">自己的appid</span>]</span><br><span class="line">  <span class="attr">appkey:</span> [<span class="string">自己的appkey</span>]</span><br><span class="line">  <span class="attr">notify:</span> <span class="literal">false</span> <span class="comment"># 因为常看blog，就不用邮件提醒了</span></span><br><span class="line">  <span class="attr">verify:</span> <span class="literal">false</span> <span class="comment"># 不会有人在我这刷评论吧？</span></span><br><span class="line">  <span class="attr">placeholder:</span> <span class="string">(((*°▽°*)八(*°▽°*)))</span> <span class="comment"># 可可爱爱</span></span><br><span class="line">  <span class="attr">avatar:</span> <span class="string">https://www.gravatar.com/avatar/[自己的hash]</span> <span class="comment">#Gravatar的头像链接或者mp这种通用头像</span></span><br><span class="line">  <span class="attr">language:</span> <span class="string">zh-cn</span></span><br></pre></td></tr></table></figure>
<p>然后<code>hexo clean &amp;&amp; hexo g</code>就可以了！</p>
<p>大概长这个样子：</p>
<img data-src="/2021/02/05/%E6%9B%B4%E6%8D%A2hexo%E7%9A%84%E8%AF%84%E8%AE%BA%E7%B3%BB%E7%BB%9F/image-20210205175008543.png" class>
<p>清爽多了。</p>
<p>又水了一篇blog。</p>
]]></content>
      <categories>
        <category>博客</category>
      </categories>
  </entry>
  <entry>
    <title>期末出分有感</title>
    <url>/2021/01/22/%E6%9C%9F%E6%9C%AB%E5%87%BA%E5%88%86%E6%9C%89%E6%84%9F/</url>
    <content><![CDATA[<p>出分了，花了好久才挤进info网站，过了一会信息化中心就把查成绩页面改成了“暂时无法提供服务”，可能服务器直接歇逼了…… GPA比起去年跌了不少，考虑到这学期整体性地不顺利，也算在预料之内。电子学听不懂，overfit了，结果就差不到1分的总评就可以上岸；大物没复习到一维无限深势阱，结果就差了这一道选择题。 但纠结这个也没有什么意义了。 怎么说呢，凭我自己个人志趣选的所有课，【除了翻车翻到姥姥家的人工神经网络以外】还都是4.0，这算是一点宽慰。 至少我以后回顾我的大二上，我会想起我头铁单刷人神、在夜里一边痛斥队友摸鱼一边调megstudio的经历；我会回忆起一边骂唐宏岩作业多一边乖乖写题的经历；我会想起对着一道写出了悬垂指针bug结果调了两天的题在gdb里不停地nnnnn的经历。 概率统计和数据结构这种正派硬课我还是认真听了、也有充分能力学好的，至于张雷和李列明的两门司马课程我还是希望它们尽快消失在历史的长河中。</p>
]]></content>
      <categories>
        <category>废话</category>
      </categories>
      <tags>
        <tag>树洞</tag>
      </tags>
  </entry>
  <entry>
    <title>《恋爱绮谈~不存在的夏天》游玩感想</title>
    <url>/2021/02/16/%E3%80%8A%E6%81%8B%E7%88%B1%E7%BB%AE%E8%B0%88-%E4%B8%8D%E5%AD%98%E5%9C%A8%E7%9A%84%E5%A4%8F%E5%A4%A9%E3%80%8B%E6%B8%B8%E7%8E%A9%E6%84%9F%E6%83%B3/</url>
    <content><![CDATA[<p>之前看过好兄弟在空间写过一次推荐，正好打算抽时间打一部gal，听说这部gal演出优秀，剧情引入入胜、流程也不很长，就steam正价入了。</p>
<a id="more"></a>
<p>前面算是很常见的要素组合：非自然研究部，自认的妹妹，博学的金牌辅助基友，完美的女友和戏份并不多的女友的弟弟。梦野瞳的L2D演出效果很好，也出色地让人觉得这是剧情中可口的点缀。编剧用了几章剧情回忆了男主和半夏经历过的那些事情，直到泳装戏水那一段，都是非常典型的恋爱轻喜剧——</p>
<p>直到那个“你老婆没了”的成就出现。</p>
<p>后面剧情出现了多次陡转。虽然没什么人看我的blog，我还是尽量不去剧透。虽然END很多（有7个），但1~6都是暴毙END，剧情本身还是单线。</p>
<p>如果要总结一下的话，”跌宕起伏，引人入胜“这种套话还是非常适合这个剧本的，只是少数章节如Chap24让我感觉非常违和，大人视角写儿童，编剧还是不太在行。</p>
<p>游戏画面本身无可置疑，是很优秀的。梦野瞳L2D的加入很新颖，效果也很好。声音我个人觉得不错，违和感比之前推国配gal要轻不少，这一定程度上要归功于优秀的localization设计。</p>
<p>标题存在双关。</p>
<p>我觉得这个游戏最让我感慨的就是，在它发售后不久，就出现了可橙被家长强制扭送到矫正中心的事件……不是每个人都能成为苏半夏，也不是每个人都能遇到自己的顾韦。这种想法还是很沉重的。</p>
<p>以上全是胡诌八扯，如有意见，欢迎开喷，我看心情接受与否。</p>
]]></content>
      <categories>
        <category>杂谈</category>
      </categories>
  </entry>
</search>
